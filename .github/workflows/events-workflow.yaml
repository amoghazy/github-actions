name: Events Workflow Demo

on:
  schedule:
    - cron: "*/15 * * * *"
  push:
    branches:
      - main
jobs:
  demo-repo-secrets:
    runs-on: ubuntu-latest

    steps:
      - name: test-Events
        run: |
          echo "Hello World"
